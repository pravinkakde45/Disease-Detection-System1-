name: disease_detection_app
description: Professional Disease Detection System Mobile App.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.0
  provider: ^6.0.5
  google_fonts: ^5.1.0
  intl: 0.20.2
  flutter_local_notifications: ^17.2.2
  timezone: ^0.9.2
  geolocator: ^10.1.0
  url_launcher: ^6.1.14
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/lang/
    - assets/logo.png
